# Implementation Plan

> Living doc. Update this file every time new architecture, module, or major decision is added. Do not let this drift from actual codebase state.

## Status

Learning project for vector data ingestion, parsing, RAG, and DB connections. Foundation layer complete: settings, env config, Pydantic data models. Ingestion layer functional for PDF, Excel/CSV, JSON, and SQL (clinical trials) sources (loaders + chunker wired via `scripts/ingest_documents.py`); JSON path includes a real data source (PMC/PubMed fetcher), SQL path includes a real data source (ClinicalTrials.gov fetcher). **All loaders and the chunker return `langchain_core.documents.Document` directly** — the earlier Pydantic `Document`/`Chunk` wrapper models were removed (2026-08-12 migration) since every real loader either already produced LangChain Documents internally (Docling, Unstructured) or gained nothing from the extra conversion step; `ChunkerService` now uses the splitter's native `split_documents()` instead of hand-rolling text splitting + index reconstruction. **Embeddings layer**: `Embedder` (`ingestion/embedder.py`) generates vectors via OpenRouter's `/embeddings` endpoint, cache-checked against Postgres via `ChunkStore` (`storage/chunk_store.py`) to skip re-paying for duplicate text. **Full ingestion→embed→Postgres→Qdrant pipeline wired**: `scripts/ingest_documents.py` runs load → chunk → embed → persist one `Chunk` row per occurrence in Postgres → sync new content_hashes to Qdrant end to end. **Postgres migration (2026-08-20)**: sqlite `EmbeddingCache`/`DocumentCache` deleted, replaced by Postgres + SQLModel (`storage/postgres.py`, `storage/chunk_store.py::ChunkStore`, `models/vectors.py::Chunk`/`FailedEmbedding`/`IngestedDocument`) — Postgres now the sole embedding cache-check + document-dedup source; Qdrant remains the sole similarity-search backend (`vector_db/qdrant.py::QdrantVectorStore.upsert_one`). RAG layer still planned. See `INGESTION_GUIDE.md` for the end-to-end usage walkthrough.

## Architecture

**Design Principle:** Modular + Composable. Each pipeline (ingestion, embedding, RAG, storage) is:

- Callable standalone with its own interface
- Chainable with other pipelines
- Independently testable and deployable

**Layers:**

1. **Settings & Config** — Environment loading (shell priority > .env)
2. **Ingestion** — Document loading and parsing (loaders, parsers, chunkers)
3. **Embeddings** — Text-to-vector conversion via embedding models
4. **Vector DB** — Storage abstraction (Qdrant local via Docker, integration layer planned)
5. **RAG** — Retriever + Generator orchestration
6. **Storage** — Relational DB connectors for metadata/context (SQLite for clinical trials, Postgres for chunk provenance/cache/dedup)
7. **Observability & Logging** — Structured logging configuration (JSON output, semantic events)
8. **Infrastructure** — Containerized services (Qdrant, Postgres, startup scripts)

## Coding Conventions

- **500-line file cap:** No `.py` file exceeds 500 lines (AGENTS.md rule #7). Crossing the cap signals a module doing too many things — split by responsibility into a subpackage, DRY shared logic into a base module, and re-export from `__init__.py` so callers' import paths don't change. Reference implementation: `ingestion/loaders/` (see Components below).

## Components

### 1. settings.py

- **Purpose:** Centralized config from environment
- **Status:** ✓ Created
- **Key files:** `settings.py`
- **Exports:** `Settings` class, `settings` singleton
- **Embeddings-related fields (added alongside `ingestion/embedder.py`):** `embedding_batch_size` (env `EMBEDDING_BATCH_SIZE`, default `100`) — max texts per OpenRouter `/embeddings` request; `embedding_model` default changed to `openai/text-embedding-3-small` (was `sentence-transformers/all-MiniLM-L6-v2`, not a valid OpenRouter slug). `.env.example` updated to match.
- **Postgres migration (2026-08-20):** `embedding_cache_db_path`/`document_cache_db_path` (sqlite paths) removed. Added `postgres_dsn` (env `POSTGRES_DSN`, default `postgresql://postgres:postgres@localhost:5432/medical_ingestion`) — backs `storage/postgres.py` engine, consumed by `ChunkStore`. `qdrant_url` and the unrelated clinical-trials `sqlite_db_path`/`clinical_trials_table`/`eligibility_table` fields kept unchanged (separate sqlite concern, not migrated).

### 1.5. logging_config.py

- **Purpose:** Application-wide structured logging configuration using `structlog` for JSON output and semantic event logging
- **Status:** ✓ Created (2026-08-20)
- **Key files:** `logging_config.py`
- **Exports:** `get_logger(name)` function returning a configured structlog logger
- **Configuration:** Uses `structlog.configure()` with:
  - Processors: `TimeStamper(fmt="iso")`, `add_log_level`, `StackInfoRenderer()`, `format_exc_info`, `UnicodeDecoder()`, `JSONRenderer()`
  - Context class: `dict` (flat, not nested)
  - Logger factory: `PrintLoggerFactory()` (writes to stdout/stderr)
  - Cache: `cache_logger_on_first_use=True`
- **Why structured logging:** Every pipeline stage (load, chunk, embed, store, Qdrant sync) now emits semantic JSON events with context fields (counts, hashes, errors, paths). Enables downstream log aggregation (ELK, Datadog, CloudWatch), querying by event type, and operational observability without parsing unstructured strings.
- **Migration from print() (2026-08-20):** All `print()` statements in `scripts/ingest_documents.py` migrated to structured logging — document/chunk/embed progress, deduplication decisions, vector store sync results now logged as semantic events with context fields instead of formatted strings.
- **Related:** [Ingest Pipeline Script](doc/feature/ingest_documents_script.md) — primary use site; see concept doc for semantic event names at each stage.

### 2. models/

- **Purpose:** Pydantic data contracts for everything that isn't a document/chunk (those are now plain `langchain_core.documents.Document` — see Ingestion below)
- **Status:** ✓ Created
- **Key files:**
  - `models/documents.py` — **no longer defines Document/Chunk/Metadata classes** (removed 2026-08-12; superseded by `langchain_core.documents.Document`, metadata is a plain dict by convention: `source`, `source_type`, `title`, `tags`, plus source-specific keys). File kept as a docstring pointer, not deleted outright, so old imports fail loudly instead of silently.
  - `models/vectors.py` — **rewritten 2026-08-20 (Postgres migration):** `Chunk` (`SQLModel(table=True)`, `chunks` table — one row per chunk *occurrence*; `content_hash` indexed but not unique, since identical text across different documents/patients intentionally produces multiple rows; `embedding` is a JSON column used only for cache-hit lookup, never searched; `metadata_` is a JSON column aliased `"metadata"`; `Chunk.make_content_hash(model, text)` staticmethod), `FailedEmbedding` (`SQLModel(table=True)`, `failed_embeddings` DLQ table), `IngestedDocument` (`SQLModel(table=True)`, `documents` table, `content_hash` primary key, whole-document dedup gate, `IngestedDocument.make_content_hash(content)` staticmethod). Old plain-Pydantic `Vector` model removed (superseded by `Chunk`). `EmbeddingRequest`/`EmbeddingResult` kept as plain Pydantic API-boundary types.
  - `models/rag.py` — RAGQuery, RAGResponse, RetrievedContext. `RetrievedContext.chunk` is now typed `langchain_core.documents.Document` (was the removed `Chunk` model).
  - `models/clinical_trial.py` — ClinicalTrial (nct_id, title, status, phase, condition, sponsor, summary, start_date, enrollment_count), Eligibility (nct_id FK, sex, minimum_age, maximum_age, std_ages, healthy_volunteers, population) — 1:1 split mirroring the API's own module boundary. Both are `SQLModel(table=True)`, not plain `BaseModel` — same class doubles as the Pydantic contract and the ORM table row; `__tablename__` is set from `settings.clinical_trials_table`/`settings.eligibility_table` rather than hardcoded.
- **Exports:** All via `models/__init__.py`
- **Convention:** Every new *non-document* data type gets a Pydantic model here; no dicts, tuples, or ad-hoc classes. Document/chunk data uses `langchain_core.documents.Document` directly (page_content + metadata dict), not a custom wrapper. Data types backed by a relational table use `SQLModel(table=True)` instead of plain `BaseModel` (see ClinicalTrial/Eligibility) so the model doubles as the ORM row.

### 4. ingestion/

- **Purpose:** Extract, parse, and chunk documents from multiple source formats into `langchain_core.documents.Document` objects
- **Status:** ✓ Created (PDF, Excel/CSV, JSON, SQL, chunking)
- **Key files:**
  - `ingestion/loaders/` — package (split from a single `loaders.py` under the 500-line file cap, see rule below): `LoaderFactory` + loader services: `PDFLoaderService` (Docling), `TextLoaderService`, `ExcelCSVLoaderService` (Unstructured), `JSONLoaderService` (nested metadata flattening + common-field content extraction for clinical/research JSON), `SQLDataLoaderService` (clinical trials from SQLite, see storage/ below). **All return `langchain_core.documents.Document` objects** (migrated 2026-08-12 from a Pydantic `Document`/`Chunk` wrapper — see Changelog). Loader classes themselves are still Pydantic `BaseModel`s (config/validation), only the *output type* changed. Public API unchanged — `from ingestion.loaders import LoaderFactory` still works via `__init__.py` re-exports. File-by-file breakdown: [doc/feature/loaders.md](doc/feature/loaders.md).
  - `ingestion/chunker.py` — `ChunkerService` + `ChunkerConfig`. Splits Documents via `RecursiveCharacterTextSplitter.split_documents()` (LangChain-native — metadata propagation and `start_index` tracking now come from the splitter itself, no more manual `str.find()` index reconstruction). `chunk_size`/`chunk_overlap` default from `settings.chunk_size`/`settings.chunk_overlap`.
  - `ingestion/clinicaltrials_fetcher.py` — fetches from ClinicalTrials.gov API v2, parses into `ClinicalTrial`/`Eligibility` SQLModel instances (see storage/ below); feeds `SQLDataLoaderService` via the SQLite DB, not directly. Live, used by `scripts/seed_clinical_trials_db.py` — stays at `ingestion/` root, not a reference script.
  - `ingestion/loaders/individual-scripts/` — standalone reference/demo scripts, physically relocated here from the `ingestion/` root (paths below reflect current location; not part of the `LoaderFactory` class hierarchy, none are imported by `scripts/`):
    - `pdf_extraction.py` — standalone reference PDF loader (Docling); architecturally identical in output type to `PDFLoaderService` (both return LangChain Documents), kept as a simpler non-Pydantic-class reference implementation, not used by `scripts/ingest_documents.py`.
    - `json_extraction.py` — standalone reference JSON loader; same relationship to `JSONLoaderService` as above.
    - `excel_csv_extraction.py` — standalone reference Excel/CSV loader (Unstructured); same relationship to `ExcelCSVLoaderService` as above.
    - `basic_document_ingestion.py` — earliest reference script, plain-text load + chunk with no Pydantic wrapping at any point.
    - `pmc_fetcher.py` — fetches PMC/PubMed full-text papers, writes flat `.txt` files.
    - `pmc_json_converter.py` — fetches papers from PMC/PubMed Central E-utilities (search + full-text XML fetch) and writes them as structured JSON (`dummy_docs/pmc_documents.json`) matching the schema `JSONLoaderService` expects (`publication_info`, `authors`, `research_data`, etc.). Populates real sample data for the JSON ingestion path.
- **Data flow:** JSON — `ingestion/loaders/individual-scripts/pmc_json_converter.py` → `dummy_docs/*.json` → `JSONLoaderService` → `Document` → `ChunkerService.chunk()` → `Document` (chunked). SQL — `ingestion/clinicaltrials_fetcher.py` → SQLite (via `storage.SQLLoaderService`) → `SQLDataLoaderService` → `Document` → `ChunkerService.chunk()` → `Document` (chunked). Every source converges on the same `Document` type before chunking.

### 4a. scripts/

- **Purpose:** Executable entry points that wire ingestion components together
- **Status:** ✓ Created
- **Key files:** `scripts/ingest_documents.py` — full pipeline script (load JSON/PDF/CSV-Excel → chunk → summary stats)
- **Fixed (2026-08-12):** `scripts/ingest_documents.py` previously called `ChunkerConfig(chunk_size=..., chunk_overlap=..., separators=[...])` and `RecursiveChunker(...)`, neither of which exist in `ingestion/chunker.py`. Now uses `ChunkerConfig(chunk_size, chunk_overlap)` + `ChunkerService(config=config)`, matching the real classes (commit `91e97b4`).

### 5. ingestion/embedder.py + storage/chunk_store.py + storage/postgres.py

- **Purpose:** Text-to-vector conversion via OpenRouter's `/embeddings` endpoint, with a Postgres-backed cache-check so identical text is never re-embedded (re-paid for) twice, while still persisting one row per real chunk occurrence for provenance. Includes retry logic for resilience and a Dead-Letter Queue (DLQ) for failed embeddings.
- **Status:** ✓ Created and wired into `scripts/ingest_documents.py` — full load → chunk → embed → Postgres → Qdrant pipeline.
- **Key files:**
  - `ingestion/embedder.py` — `Embedder` class. `__init__(model=None, batch_size=None, chunk_store=None)` defaults from `settings.embedding_model`/`settings.embedding_batch_size`/a new `ChunkStore(storage.postgres.engine)`. `embed(texts)` computes a content hash per text (`Chunk.make_content_hash`), looks up hits in bulk via `chunk_store.find_by_hash()`, batches only the cache misses to the OpenRouter API (`settings.embedding_batch_size` per request), and returns vectors in original input order (not batch/cache order). **No longer writes any cache entry itself** (2026-08-20 migration — a real behavior change: it used to write via `cache.set_many()`; persistence is now the ingest script's job). `embed_one(text)` and `embed_with_hashes(texts)` are convenience wrappers. Raises `EmbedderError` on non-2xx response or a response/request length mismatch. **Retry strategy (added 2026-08-13):** `_embed_batch()` decorated with `@retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=2, max=10), reraise=True)` — 3 retries with exponential backoff (2-10 seconds) on transient API failures. Failed batches after retries are logged to the DLQ via `chunk_store.add_failed()` before re-raising.
  - `storage/postgres.py` — SQLAlchemy `engine` from `settings.postgres_dsn`, `init_db()` calling `SQLModel.metadata.create_all(engine)`. No Alembic — matches `storage/sql.py`'s no-migrations pattern.
  - `storage/chunk_store.py` — `ChunkStore` repository class, **replaces the deleted `storage/embedding_cache.py` (`EmbeddingCache`) and `storage/document_cache.py` (`DocumentCache`)**: `find_by_hash(content_hash) -> list[float] | None` (Postgres SELECT — the sole embedding cache-check source now; Qdrant is no longer queried during embedding), `insert_chunks(rows)` (bulk insert, always one row per chunk occurrence even on cache-hit, caller batches ~100), `sync_to_qdrant(rows, qdrant_store)` (upserts to Qdrant only for content_hashes not already present there, dedupes within the batch too, one point per unique hash), `add_failed`/`get_failed` (DLQ), `document_seen`/`mark_document_seen` (whole-doc dedup, replaces `DocumentCache`). Exported from `storage/__init__.py` alongside `SQLLoaderService`.
- **Why the migration (sqlite → Postgres):** the old dedup keyed embeddings only on `(model, text)`, ignoring metadata — identical clinical text appearing in Patient A's file, Patient B's file, and a clinical-trial document collapsed to one Qdrant point, losing patient/document lineage. New design still dedupes the expensive OpenRouter API call (Postgres content_hash lookup via `find_by_hash`), but persists one Postgres row per `(content, occurrence)` so provenance is queryable, e.g. "all Type 2 Diabetes mentions for Patient A" via `SELECT ... WHERE metadata->>'patient_mrn' = ...`.
- **Why the retry strategy:** API timeouts and transient failures are common in high-throughput ingestion pipelines; exponential backoff reduces thundering-herd and gives the provider time to recover. EmbedderError only propagates after retries are exhausted, so transient hiccups don't immediately crash the pipeline.
- **Data flow:** [Chunker](doc/feature/chunker.md) → `Document` (chunked) → `[doc.page_content for doc in chunks]` → `Embedder.embed_with_hashes()` → Postgres cache lookup via `ChunkStore.find_by_hash()` (hit → skip API; miss → retry-wrapped `_embed_batch()` → OpenRouter API) → `list[list[float]]` + hashes (same order as input) → `scripts/ingest_documents.py::embed_and_store` builds `Chunk` rows → `chunk_store.insert_chunks()` (Postgres, always) → `chunk_store.sync_to_qdrant()` (Qdrant, only new hashes); failure: DLQ write via `chunk_store.add_failed()` → EmbedderError raised.

### 6. infrastructure/

- **Purpose:** Containerized services and startup automation
- **Status:** ✓ Created (Qdrant, Postgres)
- **Key files:**
  - `docker-compose.qdrant.yml` — Docker Compose spec for Qdrant v1.19+, local development: `medical-ingestion-qdrant` container, ports 6333 (HTTP API) + 6334 (gRPC), persistent volume `qdrant_storage`
  - `scripts/start_qdrant.sh` — idempotent startup script: checks Docker daemon, tries `docker compose` then `docker-compose` CLI (fallback: bare `docker run`), starts/resumes existing container or creates new one, prints dashboard URL
  - `docker-compose.postgres.yml` — plain `postgres:16` image (no pgvector extension), mirrors the Qdrant compose file's pattern. Added 2026-08-20 for `ChunkStore`'s backing store.
  - `scripts/start_postgres.sh` — mirrors `scripts/start_qdrant.sh`'s idempotent startup pattern.
  - **Rationale:** Qdrant is the sole vector-search backend (see `vector_db/` below); Postgres is relational-only (chunk provenance/cache/dedup), no vector extension — see the "pgvector rejected" settled decision below. Docker+Compose unifies local dev environment — no manual setup, reproducible across team + CI/CD.
  - **Usage:** `bash scripts/start_qdrant.sh` — Qdrant at `http://localhost:6333` (HTTP) / `localhost:6334` (gRPC). `bash scripts/start_postgres.sh` — Postgres at `localhost:5432`.

### 6a. vector_db/

- **Purpose:** Vector store abstraction + Qdrant integration (wire chunked embeddings → Qdrant, expose retrieval API)
- **Status:** ✓ Created
- **Key files:** `vector_db/base.py` — abstract `VectorStore`: `upsert_one(content_hash, embedding, metadata, text)` (single point, caller guarantees the hash is new to the store) + `find_by_hash(content_hash)`. `vector_db/qdrant.py` — `QdrantVectorStore` implementation. **Interface simplified 2026-08-20:** the old `upsert(chunks, embeddings, content_hashes)` (always-new-point-per-call, dedup-scan done inside Qdrant) was replaced by `upsert_one` — dedup-checking is now the caller's job (`ChunkStore.sync_to_qdrant`, which checks `find_by_hash` before calling `upsert_one`), keeping the Qdrant client itself simple and stateless about dedup policy.

### 7. rag/ (planned)

- **Purpose:** Retrieval + generation
- **Key files:** `retriever.py`, `generator.py`, `pipeline.py`

### 8. storage/

- **Purpose:** Relational DB persistence — schema, seed, query only. Two independent backends: SQLite (clinical trials, via SQLModel) and Postgres (chunk provenance/cache/dedup, via SQLModel — see Components #5 above for `storage/chunk_store.py`/`storage/postgres.py`). Neither knows about `Document`/chunking directly; that conversion lives in `ingestion/loaders/sql_loader.py` for the SQLite path (see below), matching how PDF/Excel/JSON keep loading separate from storage.
- **Status:** ✓ Created (clinical trials on SQLite; chunk provenance/cache/dedup on Postgres, added 2026-08-20)
- **Key files (clinical trials / SQLite):**
  - `storage/sql.py` — `SQLLoaderService`, built entirely on **SQLModel** (`Session` + `select()`), no raw SQL strings anywhere in this file. `__init__` creates a `sqlalchemy.Engine` (`sqlite:///<db_path>`) and calls `SQLModel.metadata.create_all()` — table DDL comes from the `ClinicalTrial`/`Eligibility` model classes themselves, not a hand-written schema. `seed(trials, eligibility_records)` uses `session.merge()` (upsert) for both tables. `query(*predicates)` / `query_with_eligibility(*predicates)` take SQLModel column expressions (e.g. `ClinicalTrial.status == "RECRUITING"`) instead of raw WHERE-clause strings — this is the ORM boundary: swapping the SQLite engine for Postgres/pgvector later only changes the `create_engine()` URL, callers never touch SQL. `query_with_eligibility()` does a `select(ClinicalTrial, Eligibility).join(..., isouter=True)` and returns `(ClinicalTrial, Eligibility | None)` tuples — no `Document` conversion here (moved out 2026-08-12, see Changelog).
  - `ingestion/loaders/sql_loader.py` — `SQLDataLoaderService` (registered in `LoaderFactory.sql_loader()`), wraps `storage.SQLLoaderService`, calls `query_with_eligibility()`, and converts the joined result into `langchain_core.documents.Document` with **fully flattened metadata** — every `ClinicalTrial`/`Eligibility` field is its own scalar top-level metadata key (`eligibility_sex`, `eligibility_minimum_age`, etc.), no nested dicts, since most vector-store metadata filters (Chroma included) only support flat scalar values.
  - `ingestion/clinicaltrials_fetcher.py` — fetches studies from ClinicalTrials.gov API v2 (`query.cond` search), `parse_trial()` and `parse_eligibility()` split one `protocolSection` JSON into `ClinicalTrial` + `Eligibility` model instances; `fetch_dummy_trials()` returns both lists, 1:1 by nct_id
  - `scripts/seed_clinical_trials_db.py` — fetches trials for a fixed condition list (diabetes, cancer, hypertension, asthma, alzheimer) and seeds `settings.sqlite_db_path` (default `./data/medical.db`); imports `SQLLoaderService` from `storage.sql`, unaffected by the loader-layer split.
- **Data flow:** `clinicaltrials_fetcher.py` → `(ClinicalTrial, Eligibility)` model instances → `SQLLoaderService.seed()` (SQLModel `session.merge()`) → SQLite (2 tables, FK, DDL from model classes) → `SQLLoaderService.query_with_eligibility()` (SQLModel `select().join()`) → `SQLDataLoaderService.load()` (flatten to `Document`) → `ChunkerService.chunk()` → `Document` (chunked)
- **Table names configurable:** `settings.clinical_trials_table` / `settings.eligibility_table` (env vars `CLINICAL_TRIALS_TABLE`/`ELIGIBILITY_TABLE`, default `clinical_trials`/`eligibility`) set `__tablename__` on the model classes — renaming tables never requires touching `storage/sql.py`.

## Data Sources

**Free medical data APIs for testing/sample data:**

- **PubMed E-utilities** (`https://eutils.ncbi.nlm.nih.gov/entrez/eutils/`) — 35M+ peer-reviewed papers; no key; 3 req/sec limit; methods: esearch (find), efetch (metadata + abstract), elink (related)
- **PMC Open Access** (~2M full-text papers via PubMed db=pmc) — freely available complete articles; same rate limits; use for full paper content
- **ClinicalTrials.gov API v2** (`https://clinicaltrials.gov/api/v2/`) — 500K+ trials; normalized JSON; search by condition/drug/phase/sponsor

**Paper ingestion strategy:** Use PubMed for metadata + abstracts; combine with PMC full-text for open-access papers. Fall back to abstract-only for restricted-access papers.

**Sample data storage:** All dummy/test data in `dummy_docs/` — organized by source (pubmed/, clinicaltrials/) with format indicators in filenames. Example: `full_paper_diabetes.txt` (combined PubMed metadata + PMC full text).

## Open Decisions

- **RAG retrieval strategy:** Hybrid (vector + metadata filters)
  - Alternatives: Dense-only, BM25 hybrid, multi-stage
- **Vector DB abstraction layer beyond Qdrant:** `vector_db/base.py`'s `VectorStore` interface exists and is implemented by Qdrant, but has never been exercised against a second backend (Chroma, FAISS, etc.) — the interface shape (`upsert_one`, `find_by_hash`) is informed entirely by Qdrant's own semantics so far. Whether it generalizes cleanly to a different backend is still untested. (Narrowed 2026-08-20 — the interface itself is now settled/simplified, see Architecture; what remains open is multi-backend support, not the interface's existence.)

**Settled (moved from Open Decisions to Architecture):**

- **Chunk size/overlap source of truth:** `settings.py` (`CHUNK_SIZE`/`CHUNK_OVERLAP` env vars, defaults 1024/100), not hardcoded in `ChunkerConfig`. (inferred from code, `ingestion/chunker.py`)
- **JSON as first-class ingestion source:** JSON is treated on par with PDF and Excel/CSV, with `JSONLoaderService` doing rich nested-metadata extraction for clinical/research-paper schemas (patient_info, clinical_data, publication_info, authors, research_data, surgical_data). (inferred from code, `ingestion/loaders.py`)
- **ORM for relational storage:** `storage/` uses SQLModel (Pydantic + SQLAlchemy) throughout — originally just `clinical_trials`/`eligibility` (SQLite), chosen so query code would stay stable if the backend later moved to Postgres. That move has now happened for a second, independent table set: `chunks`/`failed_embeddings`/`documents` (Postgres, `storage/chunk_store.py` + `storage/postgres.py`, added 2026-08-20) also use SQLModel — the two SQLModel usages are separate engines/DBs (SQLite for clinical trials, Postgres for chunk provenance), not a merged schema. Table names live in `settings.py`, not hardcoded, for the same portability reason. (`models/clinical_trial.py`, `storage/sql.py`, `models/vectors.py`, `storage/chunk_store.py`, `storage/postgres.py`)
- **pgvector rejected — Qdrant kept as the sole vector-search backend (2026-08-20):** Considered pgvector for combined relational+vector storage in Postgres, reversed mid-design. Postgres's `chunks.embedding` column is a plain JSON list, used **only** for cache-hit-avoidance lookups (`ChunkStore.find_by_hash`, skip re-paying OpenRouter for duplicate text) — never queried for similarity. Qdrant (`vector_db/qdrant.py::QdrantVectorStore`) remains the only similarity-search backend; `docker-compose.postgres.yml` uses a plain `postgres:16` image, no pgvector extension. Final split: Postgres owns relational provenance/cache, Qdrant owns vector search — no overlap. (`storage/postgres.py`, `docker-compose.postgres.yml`, migration description)
- **Vector store interface simplified to `upsert_one` (2026-08-20):** `vector_db/base.py::VectorStore.upsert(chunks, embeddings, content_hashes)` (always-new-point-per-call, dedup-scan inside the store) replaced by `upsert_one(content_hash, embedding, metadata, text)` (single point, caller guarantees the hash is new). Dedup-checking moved out of the vector store and into the caller (`ChunkStore.sync_to_qdrant`, which checks `find_by_hash` before calling `upsert_one`) — the vector store itself no longer needs to know about occurrence-level provenance or dedup policy, that's Postgres's job now. This effectively settles part of the old "Vector DB abstraction layer" open question: the interface shape is now Qdrant's semantics, single-point-write + hash-lookup, but has not yet been validated against a second backend (still open, see Open Decisions).
- **Primary embedding provider/model:** OpenRouter, `openai/text-embedding-3-small` (`settings.embedding_model`, env `EMBEDDING_MODEL`) — the previous default, `sentence-transformers/all-MiniLM-L6-v2`, was not a valid OpenRouter slug (that's a local/HF model, not something OpenRouter serves) and has been replaced. (inferred from code, `ingestion/embedder.py`, `settings.py`)
- **Embedding cache/dedup moved from raw `sqlite3` to Postgres + SQLModel (2026-08-20, supersedes prior sqlite decision):** `storage/embedding_cache.py` (raw sqlite3, single key-value table) and `storage/document_cache.py` (raw sqlite3, whole-doc dedup) both deleted. Replaced by `storage/chunk_store.py::ChunkStore` against Postgres (`storage/postgres.py`), using SQLModel throughout — the driving need was per-occurrence provenance (patient/document lineage), which a flat key-value cache structurally can't hold. See the "pgvector rejected" decision above for why this is Postgres, not Qdrant, and why it's not pgvector either. (`storage/chunk_store.py`, `storage/postgres.py`, `models/vectors.py`)
- **Retry strategy for embeddings:** `_embed_batch()` uses tenacity with 3 attempts and exponential backoff (2-10s multiplier=1) to handle transient API failures. Failed batches are logged to a `failed_embeddings` DLQ table (not auto-retried in the same call) for later inspection. This trades latency upfront (shorter wait on first failure, bounded retries) for observability and recoverability (failed texts never silently disappear). (inferred from code, `ingestion/embedder.py`, commit 293332b; DLQ table now lives in Postgres, `models/vectors.py::FailedEmbedding`, since 2026-08-20)
- **Dead-Letter Queue for failed embeddings:** `failed_embeddings` table, now in Postgres (`models/vectors.py::FailedEmbedding`, migrated 2026-08-20 from the sqlite `embedding_cache.db` file), tracking content_hash, text, error, model, attempt_count, and created_at. Populated by `Embedder.embed()` via `chunk_store.add_failed()` when a batch fails after all retries. Retrievable via `ChunkStore.get_failed(limit)` for operational inspection, debugging, and optional manual retry workflows.
- **Primary vector DB: Qdrant (local via Docker):** Local development and testing use containerized Qdrant v1.19+ (`docker-compose.qdrant.yml`), running on 6333 (HTTP)/6334 (gRPC). Persistent storage via named volume `qdrant_storage`. Startup automated via `scripts/start_qdrant.sh` (composite: Docker Compose if available, fallback to `docker run`). Chosen for simplicity (single binary, no extra services), persistence (unlike in-memory FAISS), and feature completeness (collections, metadata filtering, batch operations). Remains the sole similarity-search backend even after the Postgres migration — see "pgvector rejected" above. (inferred from code, `docker-compose.qdrant.yml`, `scripts/start_qdrant.sh`, commit 6083093)
- **Parsing library split:** Docling for scientific PDFs (PubMed/PMC papers) — strong table/formula extraction via TableFormer, native hierarchical output for structure-aware chunking. Unstructured for mixed/messy file types (PDF/DOCX/PPTX/HTML/EML/images) as a fallback — format-agnostic `partition()` with auto OCR/layout analysis. (inferred from code, `ingestion/loaders/pdf.py`, `ingestion/loaders/excel_csv.py`)

## Future Enhancements (Deferred — not yet built)

Retrieval architecture decisions from design discussion, deferred until core pipeline (ingestion → embed → store) is working end-to-end:

- **Chunking — structure-aware refinement:** Current default: RecursiveCharacterTextSplitter, 1024 char chunks, 100 char overlap (settings.py defaults). Future: pre-split by section structure (PMC XML tags for papers, markdown headings, HTML divs) before recursive-split within each section, when source structure is available.
- **Contextual retrieval:** prepend each chunk with LLM-generated (Haiku) short context blurb before embedding — cuts retrieval failure rate significantly (Anthropic's technique).
- **Hybrid retrieval + reranking (enterprise-grade baseline, domain-agnostic):** dense (vector) + sparse (BM25) retrieval, RRF-fused, top-50 → cross-encoder rerank → top-5-10 → LLM. Non-negotiable at enterprise scale — dense-only misses exact-term matches (IDs, codes, dosages, citations).
- **Metadata — known case:** when metadata is knowable (dates, client/policy/event tags), store directly, pre-filter before vector search (pre-filter, not post-filter — shrinks ANN search space, avoids recall bugs).
- **Metadata — unknown case (schema discovery):** two-pass — (1) sample corpus, frontier model (Opus) proposes taxonomy/schema, human review; (2) bulk-tag all docs/chunks against fixed schema via Haiku + Batch API + structured outputs (`output_config.format`). Re-run discovery periodically (schema drift).
- **Metadata — worst case (messy dump, no structure, no timestamps):** embed everything first (needed anyway) → unsupervised clustering (HDBSCAN/k-means) on embeddings to discover latent doc-type structure without any prior metadata → sample representatively *per cluster* (not randomly) for frontier-model schema discovery → bulk-tag cheap as above → escalate to frontier model per-doc only for the low-confidence/outlier tail. Do NOT brute-force every doc through a frontier model — clustering-informed sampling gets equivalent schema quality at a fraction of the cost.
- **Generic entity extraction:** spaCy (`en_core_web_lg`) for universal entities (PERSON, ORG, DATE, MONEY, GPE) — free, fast, local, bulk pass across all docs. For medical-specific text: scispaCy (`en_ner_bc5cdr_md` etc.) for DISEASE/CHEMICAL/GENE entities generic spaCy misses. Runs alongside/before LLM-based schema tagging, not instead of it.
- **Structure-free hierarchical retrieval (RAPTOR):** cluster chunks → LLM-summarize clusters → recurse upward to a summary tree; query traverses top-down. Consider as accuracy upgrade once flat hybrid+rerank baseline is proven — high-value for accuracy-first requirements.
- **Latency levers (in priority order once accuracy baseline works):** metadata pre-filtering (biggest single win, shrinks search space before expensive stages), fewer rerank candidates / faster reranker (reranking dominates retrieval-side latency, not vector search), parallel dense+sparse search, semantic query caching.

## Changelog

- 2026-08-20: doc-sync — structured logging migration (commit 0f58b0a). Migrated all `print()` statements in `scripts/ingest_documents.py` to structured logging via a new `logging_config.py` module (singleton `structlog` setup with JSON output, ISO timestamps, processors for exceptions, `PrintLoggerFactory` to stdout/stderr). Added `structlog>=24.4.0` dependency. Every pipeline stage (load, chunk, embed, store, Qdrant sync) now emits semantic JSON events with context fields: `loading_*_documents`, `loaded_*_documents`, `filtering_seen_documents`, `chunking_documents`, `chunked_documents`, `embedding_chunks`, `embedded_chunks`, `storing_chunk_rows`, `inserted_chunk_rows`, `synced_qdrant`, `dropped_duplicate_rows`, `ingestion_summary`, etc. All event names reflect the actual operation (no generic "info" strings), all context fields are key-value pairs (counts, hashes, paths, errors), enabling downstream log aggregation and querying. Created new Components entry 1.5 (`logging_config.py`). Updated Architecture Layers to add "Observability & Logging" (layer 7). Created `doc/feature/logging_config.md` and linked from `doc/feature/index.md`. Updated `doc/feature/ingest_documents_script.md` Flow section to document semantic event names at each stage and History section to note the migration.
- 2026-08-20 (late): doc-sync — documented per-run chunk dedup logic in `scripts/ingest_documents.py` (lines 134–146). The `embed_and_store()` function now deduplicates chunk rows within the same ingestion run by `(content_hash, metadata)` pair before calling `chunk_store.insert_chunks()` — identical text appearing multiple times in the current batch (e.g., from different pages of the same multi-part PDF) is stored only once, but still one row per *unique* occurrence across different runs/documents. Updated `doc/feature/ingest_documents_script.md` step 5 to document this pre-filter step and updated `doc/feature/chunk_store.md` Components section to clarify that `insert_chunks()` receives pre-deduplicated rows from the ingest script.
- 2026-08-20: doc-sync — synced the sqlite→Postgres+SQLModel migration (Qdrant untouched). Deleted `storage/embedding_cache.py`/`storage/document_cache.py`; added `storage/postgres.py` (engine/`init_db()`) and `storage/chunk_store.py::ChunkStore` (find_by_hash cache-check, insert_chunks provenance, sync_to_qdrant, DLQ, whole-doc dedup). `models/vectors.py` rewritten: `Chunk`/`FailedEmbedding`/`IngestedDocument` `SQLModel(table=True)` classes replace the old plain-Pydantic `Vector`; `EmbeddingRequest`/`EmbeddingResult` kept. `vector_db/base.py`/`qdrant.py` simplified `upsert` → `upsert_one` (caller-guaranteed-new-hash, dedup moved to `ChunkStore`). `ingestion/embedder.py` and `scripts/ingest_documents.py` rewired accordingly (Embedder no longer writes cache entries — read-only now; ingest script owns persistence). Settings: added `postgres_dsn`, removed `embedding_cache_db_path`/`document_cache_db_path`. Updated IMPLEMENTATION_PLAN.md Components #2 (models/vectors.py), #5 (renamed to embedder.py + chunk_store.py + postgres.py), #6 (infrastructure, added Postgres), #6a (vector_db/, moved planned→created, documented interface simplification), #8 (storage/, split SQLite/Postgres), Architecture settled decisions (added "pgvector rejected, Qdrant kept," "vector store interface simplified," updated ORM decision to cover both SQLite and Postgres SQLModel usages, replaced sqlite-specific cache/DLQ/concurrency decisions with their Postgres equivalents, removed the now-inapplicable WAL-mode-concurrency decision), narrowed the "Vector DB abstraction layer" Open Decision to "beyond Qdrant, untested against a 2nd backend" since the interface itself is now settled. Created `doc/feature/chunk_store.md` and `doc/feature/postgres_storage.md`; marked `doc/feature/embedding_cache.md` `status: deprecated` (kept as a redirect stub, not deleted, since old links to it exist); updated `doc/feature/embedder.md`, `doc/feature/models.md`, `doc/feature/qdrant_infrastructure.md`, `doc/feature/ingest_documents_script.md` for the new Postgres-backed flow; updated `doc/feature/index.md` links. `AGENTS.md` and `.env.example` diffs already present in the working tree at task start were verified accurate (no old `EmbeddingCache`/`DocumentCache` references remain in either) — no further changes needed to `AGENTS.md`. **Flagged for user cleanup:** none found — `storage/sql.py`'s "Why SQLModel" rationale (moving to Postgres/pgvector) is now partially realized (Postgres, but not pgvector, and for a different table set) but reads fine as historical context; no dead code detected referencing deleted sqlite cache modules.
- 2026-08-13: doc-sync — added Qdrant infrastructure (docker-compose + startup script): moved Vector DB from "planned" to "created (Qdrant)", added new `infrastructure/` Components section documenting `docker-compose.qdrant.yml` (v1.19+, persistent volume, HTTP/gRPC ports) and `scripts/start_qdrant.sh` (composite CLI: docker compose → docker-compose → docker run fallback), updated Architecture Layers to list Infrastructure, settled the "Primary vector DB" decision (Qdrant local via Docker), updated Open Decisions to remove the closed "Default vector DB" question and add "Vector DB abstraction layer" (future `vector_db/` interface). Updated Status to note "Vector DB infrastructure in place". Created new OKF concept doc `doc/feature/qdrant_infrastructure.md` and linked from `doc/feature/index.md`. No agents or dependencies affected; Qdrant container is standalone (Docker-only, no Python package).
- 2026-08-13: doc-sync — added embedder optimizations from commit 293332b to the plan: documented tenacity retry strategy (`_embed_batch()` with 3 attempts + exponential backoff 2-10s), WAL-mode concurrency (autocommit + thread-safe concurrent access), and DLQ pattern (`failed_embeddings` table for persistent failure tracking). Updated Components entry 5 (ingestion/embedder.py + storage/embedding_cache.py) to describe retry decorator, DLQ integration, WAL mode, and concurrent access. Added 4 new settled decisions to Architecture section: retry strategy rationale (transient failure resilience + observability), concurrency rationale (parallelized ingestion throughput), DLQ design (failed text + error persistence for inspection), and whitespace normalization in cache keys. Updated `doc/feature/embedder.md` and `doc/feature/embedding_cache.md` to detail retry logic, concurrency, and DLQ. Updated `doc/feature/index.md` links (no new files, only content updates). CLAUDE.md already has Rule #8 (uv dependency management) — verified no additional update needed.
- 2026-08-12: doc-sync — added Embeddings layer to the plan: moved `embeddings/` from "planned" to created under a new Components entry `ingestion/embedder.py` + `storage/embedding_cache.py` (`Embedder`, `EmbeddingCache`), updated Status, moved "Primary embedding model" out of Open Decisions (settled: OpenRouter `openai/text-embedding-3-small`) into Architecture's settled-decisions list, added the raw-sqlite3-vs-SQLModel rationale for the new cache as a settled decision, and documented the new `settings.py` fields (`embedding_batch_size`, `embedding_cache_db_path`, changed `embedding_model` default) in the settings.py Components entry. Added `doc/feature/embedder.md` and `doc/feature/embedding_cache.md` concept docs, cross-linked to each other, to [Chunker](doc/feature/chunker.md) (upstream producer) and [SQL Loader](doc/feature/sql_loader.md) (contrast case for the ORM-vs-raw-sqlite3 decision), and linked both from `doc/feature/index.md`. Flagged: `Embedder` is not yet called from `scripts/ingest_documents.py` — pipeline still stops at chunking, embeddings layer exists but is unwired.
- 2026-08-12: doc-sync — synced `doc/feature/` with the Pydantic-Document→`langchain_core.documents.Document` migration described below. Rewrote `doc/feature/models.md` (removed `Document`/`Chunk`/`Metadata` class docs, documented `models/documents.py` as a stub, `RetrievedContext.chunk` retype), `doc/feature/chunker.md` (replaced the `str.find()`/manual index-reconstruction description with `split_documents()`), `doc/feature/loaders.md` (all 5 loaders now documented as returning `langchain_core.documents.Document`, `.page_content`/`.metadata[...]` access pattern, added `sql_loader.py` to the file list), and `doc/feature/sql_loader.md` (split into `storage/sql.py::SQLLoaderService`, now pure persistence with `.load()` removed, and `ingestion/loaders/sql_loader.py::SQLDataLoaderService`, the new `Document`-producing loader). Also found and fixed drift the migration prompt didn't mention: the standalone reference/fetch scripts (`pdf_extraction.py`, `json_extraction.py`, `excel_csv_extraction.py`, `basic_document_ingestion.py`, `pmc_fetcher.py`, `pmc_json_converter.py`, `clinicaltrials_fetcher.py`) physically moved from `ingestion/` to `ingestion/loaders/individual-scripts/` (visible in `git status` as deletions from `ingestion/` + untracked files under the new path) but every concept doc and this plan still referenced the old `ingestion/*.py` paths — updated all `resource:` frontmatter, prose paths, and CLI run commands across `doc/feature/pdf_extraction.md`, `json_extraction.md`, `excel_csv_extraction.md`, `basic_document_ingestion.md`, `pmc_fetcher.md`, `pmc_json_converter.md`, `clinicaltrials_fetcher.md`, this plan's ingestion/ and storage/ Components sections, and `doc/feature/index.md`'s concept list. Updated `doc/feature/ingest_documents_script.md` for the `Document` import-source and attribute-access changes (`doc.page_content`, `doc.metadata.get('title')`, no `doc.id`).
- 2026-08-12: **Migrated the entire loader→chunker path from Pydantic `Document`/`Chunk` to `langchain_core.documents.Document`.** Scope: all 5 loaders (`PDFLoaderService`, `TextLoaderService`, `ExcelCSVLoaderService`, `JSONLoaderService`, `SQLDataLoaderService`) now return `list[langchain_core.documents.Document]` instead of `list[models.documents.Document]`; metadata moved from a typed `Metadata` Pydantic object (`.metadata.extra["key"]`) to a plain dict (`.metadata["key"]`). `ChunkerService.chunk()` rewritten to call the splitter's native `split_documents()` instead of `split_text()` + manual `str.find()`-based `start_idx`/`end_idx` reconstruction — LangChain now handles metadata propagation and start-index tracking (`add_start_index=True` → `metadata["start_index"]`) itself. `models/documents.py` no longer defines `Document`/`Chunk`/`Metadata` (file kept as a docstring pointer so stale imports fail loudly). `models/rag.py`'s `RetrievedContext.chunk` retyped from the removed `Chunk` to `langchain_core.documents.Document`. `storage/sql.py`'s `SQLLoaderService` also lost its `load()` method in this pass — trimmed to pure persistence (schema/seed/query), with the query→Document conversion moved into a new `ingestion/loaders/sql_loader.py::SQLDataLoaderService` (registered as `LoaderFactory.sql_loader()`), so `storage/` no longer knows about `Document` at all, matching how PDF/Excel/JSON already separate loading from storage. **Rationale:** every real loader already produced or consumed LangChain Documents internally (Docling, Unstructured both return them natively; `ExcelCSVLoaderService` was previously converting LangChain→Pydantic and throwing away metadata in the process); the Pydantic wrapper added a conversion step at every boundary for no validation benefit LangChain's `Document` (`page_content: str`, `metadata: dict`) didn't already provide. `scripts/ingest_documents.py` updated for the new types and re-verified end to end (JSON loader → chunker, 2 docs → 2 chunks, clean run). Full pipeline smoke-tested with SQL + JSON loaders combined into one chunking pass (53 chunks, `start_index` present, all metadata flat). **Scope note:** this migration only covers loaders → chunker. `embeddings/`, `vector_db/`, and `rag/` remain unbuilt/planned — nothing downstream of chunking exists yet to migrate.
- 2026-08-12: doc-sync — updated `doc/feature/sql_loader.md`, `doc/feature/models.md`, and `doc/feature/clinicaltrials_fetcher.md` for the SQLModel migration: documented `ClinicalTrial`/`Eligibility` as `SQLModel(table=True)` classes doubling as ORM table rows (settings-driven `__tablename__`), rewrote `SQLLoaderService`'s `__init__`/`seed`/`query`/`query_with_eligibility`/`load` descriptions to match the `Engine`+`Session`+`select()` implementation, and called out the breaking `(where, params) -> (*predicates)` signature change on all three public query methods with before/after call examples. Fixed a stale `doc/feature/index.md` line still describing `SQL Loader` as plain SQLite without SQLModel. `clinicaltrials_fetcher.md` updated only to note it's unaffected by the migration (fetch/parse behavior unchanged). Preserved the open-gap note (`SQLLoaderService.load()` not wired into `scripts/ingest_documents.py`) unchanged — still true, confirmed no `SQLLoaderService` calls exist in `scripts/`.
- 2026-08-12: Migrated `storage/sql.py` from raw `sqlite3` to **SQLModel** (added `sqlmodel` dependency). `ClinicalTrial`/`Eligibility` (`models/clinical_trial.py`) are now `SQLModel(table=True)` classes — same class is both the Pydantic contract and the ORM table row, `__tablename__` set from `settings.clinical_trials_table`/`settings.eligibility_table` rather than hardcoded. `SQLLoaderService.__init__` now builds a SQLAlchemy `Engine` and runs `SQLModel.metadata.create_all()` instead of executing a hand-written `CREATE TABLE` schema string. `seed()` uses `session.merge()`. `query()`/`query_with_eligibility()`/`load()` signature changed from `(where: str, params: tuple)` to `(*predicates)` — callers now pass SQLModel column expressions (e.g. `ClinicalTrial.status == "RECRUITING"`) instead of raw SQL WHERE-clause strings. Rationale: query code stays stable if the backend later moves to Postgres/pgvector. Re-seeded and verified: 50 trials + eligibility rows, join and `Document` conversion both confirmed working against the real API.
- 2026-08-12: doc-sync — updated `doc/feature/clinicaltrials_fetcher.md` and `doc/feature/sql_loader.md` (previously documented as a single flat `clinical_trials` table) to match the two-table normalization: new `Eligibility` model/table, `enrollment_count` on `ClinicalTrial`, `parse_eligibility()`, `fetch_dummy_trials()`'s breaking return-type change to a tuple, `SQLLoaderService.seed()`'s two-arg signature, new `query_with_eligibility()`, and `load()` now sourcing from the join. Extended `doc/feature/models.md`'s `ClinicalTrial` section with `Eligibility` and `enrollment_count`. Fixed a stale `doc/feature/index.md` line still describing `SQL Loader` as storing only `ClinicalTrial` records. Preserved the existing open-gap note (`SQLLoaderService.load()` not wired into `scripts/ingest_documents.py`) unchanged — still true.
- 2026-08-12: Normalized `storage/` from one flat `clinical_trials` table into two tables — `clinical_trials` + `eligibility` (nct_id FK, 1:1) — mirroring the ClinicalTrials.gov API's own `eligibilityModule` split rather than fabricating patient-level rows the API doesn't expose. Added `Eligibility` Pydantic model and `enrollment_count` field to `ClinicalTrial`; `clinicaltrials_fetcher.py` now parses both via `parse_trial()`/`parse_eligibility()`; `SQLLoaderService` gained `query_with_eligibility()` (LEFT JOIN) and `seed()`/`load()` now handle both tables.
- 2026-08-12: doc-sync — added `doc/feature/clinicaltrials_fetcher.md` and `doc/feature/sql_loader.md` concept docs for the new storage layer, extended `doc/feature/models.md` with `models/clinical_trial.py`, linked both new docs from `doc/feature/index.md`. Verified `IMPLEMENTATION_PLAN.md`'s storage/ section (pre-written by main thread) against actual code — consistent, no changes needed. Flagged: `storage/`'s `SQLLoaderService.load()` output is not yet wired into `scripts/ingest_documents.py` — a real gap, noted in `sql_loader.md`, not a doc error.
- 2026-08-12: Added `storage/` layer — `ClinicalTrial` Pydantic model, `ingestion/clinicaltrials_fetcher.py` (ClinicalTrials.gov API v2), `storage/sql.py` `SQLLoaderService` (SQLite seed/query/load), `scripts/seed_clinical_trials_db.py`. Moved storage/ from planned to created.
- 2026-08-12: doc-sync — added Coding Conventions section documenting the 500-line file cap (AGENTS.md rule #7); updated `ingestion/loaders.py` reference to `ingestion/loaders/` package (base.py, pdf.py, text.py, excel_csv.py, json_loader.py, factory.py, __init__.py), pointing to `doc/feature/loaders.md` for file-by-file detail instead of duplicating it; fixed stale `ingestion/loaders.py` reference in `doc/feature/index.md`.
- 2026-08-12: doc-sync — documented JSON ingestion path (`ingestion/json_extraction.py`, `ingestion/pmc_json_converter.py`, `JSONLoaderService`), `scripts/ingest_documents.py` pipeline entry point, and settings-driven chunker config (`ChunkerConfig` now reads `chunk_size`/`chunk_overlap` from `settings.py` instead of hardcoded defaults). Moved 2 settled decisions from Open Decisions to Architecture. Flagged a real code inconsistency in `scripts/ingest_documents.py` (references `RecursiveChunker`/`separators` that don't exist in `ingestion/chunker.py`) for follow-up.
- 2026-08-11: Documented deferred retrieval architecture (chunking, hybrid+rerank, metadata auto-discovery incl. worst-case messy-data strategy, NER, RAPTOR) — see Future Enhancements section.
- 2026-08-11: Pydantic models layer — Document, Chunk, Vector, RAGQuery/Response contracts. Added Pydantic-first rule to CLAUDE.md.
- 2026-08-11: Foundation layer — settings.py, .env.example, modular architecture design, README docs.
- 2026-08-09: Added doc-sync agent for self-healing architecture documentation.
- 2026-08-09: Project scaffolded (CLAUDE.md, AGENTS.md, doc/ folder, this plan).
